import java.util.Scanner;

public class EnchantedArtifactSearch {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

       
        int n = scanner.nextInt();

        int[] artifacts = new int[n];

      
        for (int i = 0; i < n; i++) {
            artifacts[i] = scanner.nextInt();
        }

   
        int elusiveArtifact = scanner.nextInt();

      
        boolean found = false;
        for (int i = 0; i < n; i++) {
            if (artifacts[i] == elusiveArtifact) {
                found = true;
                break;
            }
        }

        if (found) {
            System.out.println("Elusive Artifact Found!");
        } else {
            System.out.println("Elusive Artifact Not Found!");
        }
    }
}
