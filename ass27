port java.util.Scanner;

public class FamilyMatchChecker {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();

        int[] harmonyFamily = new int[n];
        int[] melodyFamily = new int[n];

        for (int i = 0; i < n; i++) {
            harmonyFamily[i] = scanner.nextInt();
        }

       
        for (int i = 0; i < n; i++) {
            melodyFamily[i] = scanner.nextInt();
        }
        boolean match = true;
        for (int i = 0; i < n; i++) {
            if (harmonyFamily[i] != melodyFamily[i]) {
                match = false;
                break;
            }
        }
        if (match) {
            System.out.println("Match Found!");
        } else {
            System.out.println("No Match Found!");
        }
    }
}
1061
